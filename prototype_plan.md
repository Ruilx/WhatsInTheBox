> 本文件已经和团队确认具体的事宜，已经通过prototype_plan.patched.md文件更新了该文件的具体已确定的内容。请做参考。
> 本文件是我经过非常谨慎的验证可行性后设计的一个最基础的项目的资源和要求，本项目由我已经考证的内容都会记录在本文件中。
> 如果在查询你的知识库或网络搜索后的信息有与该文件所述冲突或有问题的地方，可以在会话中向我反馈，我会尝试再次查证和审核。如果确实存在问题，由我来更正本文件内容。
> 如前所述，你可以在会话中提出你对该文件内容的异议，但任何情况下禁止修改此文件。
> 该文件记录着本项目最基础的已定内容，包括架构选型、交互方案以及注意事项。这个文件内容应作为最基础明确且遵守的条款。
> 该文件可能会有未定的事宜，比如有需要明确或者询问的话术，如果你遇到，不要猜测我的意图，在会话中提出你的问题和建议，由我来更新该文件的内容。

# What's In The Box (箱子里面有什么)

项目名称：箱子里面有什么 （工程名称：WhatsInTheBox）

项目介绍：一个使用Vue3作为前端和Python3.12+FastAPI作为后端的箱子物品管理的平台。

* 前端：使用Vue3和Vite，编程语言使用TypeScript，使用NodeJS作为运行时，使用npm作为包管理框架，UI界面可以使用ant-design-vue。前端界面要支持手机访问。
* 后端：使用Python3.12和FastAPI，使用venv管理Python运行时，使用pip作为包管理框架，使用pymysql连接数据库。
  * 后端路由接口格式为“module_name/controller_name/action_name”，“module_name”固定为“whatsinthebox”。
  * 后端查询数据库时不用使用orm，只需要写清楚where条件，但查询必须参数化，禁止字符串拼接防 SQL 注入。
    * pymysql 只支持 `%s` 占位符，形如 `WHERE id=%s AND deleted=0`，参数以元组/字典通过 `data` 传入。
    * 在 `base_dao` 中统一封装参数化查询（query / execute / insert / soft_delete / 分页模板），**任何位置都禁止字符串拼接 SQL**；动态筛选条件也必须走「条件片段 + 参数列表」拼装，不拼接值。
  * 暂时不使用redis等缓存。
  * 后端使用数据、业务分层的代码结构（dao 数据层 / service 业务层 / controller 接口层）。
* 存储：使用 MySQL 数据库存储。
  * **本项目不使用连接池**：改为**惰性连接 + 自管连接**（真正需要访问 DB 时才建立连接，用完即释放；断线用 `ping(reconnect=True)` 兜底）。理由：自用场景负载极低，连接池带来的依赖（如 DBUtils）与复杂度不划算。
  * 字符集与排序规则统一 `utf8mb4` + `utf8mb4_general_ci`（库、表、连接三处一致）。
  * 数据库连接参数（host/port/user/password/charset/database）写入 `.env`（不入库），并提供 `.env.example` 模板供仿配。数据库位置和链接后续补充。

前端和后端代码目录分开，分别启动。需要支持登录账号界面，但不用实现注册界面；可多端登录。

* **cookie 只存随机 token**（`secrets.token_urlsafe`），token 与用户的映射落 MySQL `session` 表；cookie 中不放用户名、不放密码 hash，避免伪造。加盐 hash 用于**密码存储**（见「账号、登录与权限」一节），不用于 cookie 值本身。

# 【箱子里面有什么】介绍
在搬家封箱后，到达一个新的地方，肯定会遗忘了箱子里面放了什么。所以在放入的时候，提前在平台上录入相应的信息，这样在新的环境中可以快速定位想要的东西，并且能够准确打开放着目标物品的箱子。
* 编号 `A01` 就是箱子的 **`name` 字段**，不额外增加 `code` 字段。
  * 编号由**用户手工填写**，系统**不自动生成**、不校验必须是「字母+两位数字」格式（该格式只是用户的书写习惯）。
  * 唯一范围 = **同一活动内唯一**（不是全局唯一）；跨活动允许出现同名箱子。
  * 编号**可以修改**；它同时是 URL 标识，改名即 URL 变化（见「注意事项」中二维码条目）。

# 箱子的属性
一个箱子可以有以下属性：
* ID
* name 箱子名称（即箱子编号，如 `A01`）
  * 【已确认】用户手填、可变、**同一活动内唯一**、系统不自动生成；同时作为 URL 路径段（`/活动名/箱子名`）。
* desc 箱子描述（默认空字符串）
* type 箱子类型 **多标签**：一个箱子可同时具有多个标签，存 **JSON 数组**字段。
  * 【已确认】预置标签（可叠加）：`主要`、`次要`、`易碎`、`需保护`、`防水`、`要求向上`、`旧箱`。筛选时按「包含某标签」过滤。
* size 箱子大小
* material 箱子材质
* parent_box_id 父箱子ID
  * 支持**多层嵌套**（不限层级）。在父箱的物品列表中，子箱作为一条「特殊物品」行展示，右侧提供「查看」操作可**钻取**进入子箱；移动端同理（同样是列表 + 钻取，不做树形展开）。
* status 箱子状态
  * 完整枚举（存储类型 `unsigned tinyint`，映射关系写入列 `COMMENT`）：

    | 值 | 枚举名 | 中文 |
    |---|---|---|
    | 0 | `open` | 打开 |
    | 1 | `folded` | 折叠 |
    | 2 | `sealed` | 封存 |
    | 3 | `in_transit` | 运输中 |
    | 4 | `damaged` | 损伤 |
    | 5 | `retired` | 淘汰 |
* serial_no 箱子串号（我来定义和录入）
  * 含义定死：这是**用户买箱子时手工填写的唯一串号**，用于钉死「这一只物理箱子」。
  * **不可变**、**不与任何 ID 联动**（与 `id`、`name` 均无关系，不参与 URL）。
  * 箱子在运输中损坏销毁后，串号随箱作废：该箱记录被**逻辑删除时，`serial_no` 一并清空**（置空），从而释放该串号值。
  * 串号唯一性同样走**应用层校验**（见「注意事项」唯一约束条目）。
* photo 照片（可选）
* thumb 缩略图（可选）
  * 【已确认】`photo` / `thumb` 均只存**相对路径**（见新增章节「照片与文件存储」）。
* note 注意和备注（比如要防摔，防水等）
* first_using_time 箱子第一次使用时间

## 折叠与空箱【已确认】
* **折叠只是一个状态标志**（`status = folded`），不是不可逆动作。箱子标题处显示「已折叠」badge。
* 业务上**只提示、不强制**：允许对**非空箱**强制折叠（前端弹出提示确认即可，不阻断）。
* 折叠后：箱内物品状态**不变**、仍然可见、仍可搜索到、仍可编辑/取出。
* **空箱判定**：箱子内物品**全部取出（或被删除/扔掉）**时判定为空。该判定仅用于「折叠时给提示」以及界面展示，不构成硬约束。

# 物品的属性
一个物品可以有以下属性：
* ID
* name 物品名称
* desc 物品描述（默认空字符串）
* type 物品类型
  * 【已确认】**自由文本**（如「衣物」「书籍」），不做字典枚举、不做多标签；筛选时按文本匹配。
* box_id 箱子ID
* photo 照片（可选）
* thumb 缩略图（可选）
* status 物品状态
  * 完整枚举（`unsigned tinyint` + 列 `COMMENT`）：

    | 值 | 枚举名 | 中文 |
    |---|---|---|
    | 0 | `in_box` | 在箱 |
    | 1 | `taken_out` | 已取出 |
    | 2 | `lent` | 借出 |
    | 3 | `damaged` | 损坏 |
    | 4 | `lost` | 遗失 |
* note 注意和备注（比如有其他附件等）

## 取出动作【已确认】
点击「取出」后：
1. 将 `item.box_id` **置空**（物品脱离箱子，不再出现在该箱物品列表中）；
2. 将 `item.status` 置为 `taken_out`（已取出）。

物品记录**不删除**，仍可在「已取出」列表（`box_id` 为空的物品视图）中查询到，可再次编辑或重新放入其他箱子。

# 活动表
一次搬家可以称为一个“活动”，每一个活动之间都是独立的。
一次活动有以下属性：
* ID
* name 活动名称
  活动名称 **全局唯一**（因为它就是 URL 前缀，见「注意事项」）。箱子名仅活动内唯一，二者范围不同，不要混淆。
* desc 活动描述（默认空字符串）
* type 活动类型（比如搬家、运输等）
* start_time 活动开始时间
* end_time 活动结束时间
* status 活动状态
* 完整枚举（`unsigned tinyint` + 列 `COMMENT`）：

    | 值 | 枚举名 | 中文 |
    |---|---|---|
    | 0 | `draft` | 草稿 |
    | 1 | `active` | 进行中 |
    | 2 | `stopped` | 已停止 |
    | 3 | `archived` | 归档 |
* note 注意和备注（活动要求等）

## 活动停止后的行为
活动置为 `stopped`（已停止）后，其下的**箱子与物品仍然可以编辑**（新增/修改/删除/取出均不受限）。「停止」只是一个状态标记，不对子资源做任何写入限制。`archived`（归档）同样不加写入限制，仅作分类展示。
# 日志表
比如查询记录，物品放置、取出等

字段结构定稿如下：

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint PK | 主键 |
| activity_id | bigint，可空 | 关联活动 |
| box_id | bigint，可空 | 关联箱子 |
| item_id | bigint，可空 | 关联物品 |
| combo_id | bigint，可空 | 关联联合物品 |
| user_id | bigint | 操作人，来自当前 session |
| action | varchar | 动作：`query` / `view` / `create` / `update` / `delete` / `take_out` / `place` / `login` / `logout` / `scan` |
| object_type | varchar | 对象类型：`activity` / `box` / `item` / `combo` |
| object_id | bigint | 对象 ID |
| detail | varchar | 备注 / 变更前后值 |
| ip | varchar，可空 | 来源 IP |
| create_time | datetime | 见「注意事项」通用字段 |
| update_time | datetime | 见「注意事项」通用字段 |
| deleted | unsigned tinyint | 见「注意事项」通用字段 |

* 【已确认】日志表按全局约定同样带 `deleted` / `create_time` / `update_time` 三个通用字段，但**实质只追加**（不做 update、不做删除），三字段仅为与其他表保持结构一致。
* 【已确认】`action` 与 `object_type` 为 **varchar**（非 tinyint），便于将来直接追加新动作而不改枚举定义。

# 关联物品表
几个物品放置在不同的位置，但使用时之间有关联，这个表通过将几个不同物品ID关联起来形成一个联合。
## 联合物品
* ID
* name 联合物品名称
* type 联合物品类型（比如配合、附件等）
* status 状态
  完整枚举（`unsigned tinyint` + 列 `COMMENT`）：

    | 值 | 枚举名 | 中文 |
    |---|---|---|
    | 0 | `normal` | 正常 |
    | 1 | `invalid` | 失效 |
* note 注意和备注（物品是后配的，或者有什么注意事项）
## 联合物品物品表
* ID
* 联合物品ID
* 物品ID
* 物品状态
* 物品联合方式
  完整枚举（`unsigned tinyint` + 列 `COMMENT`）：

    | 值 | 枚举名 | 中文 |
    |---|---|---|
    | 0 | `original` | 原装 |
    | 1 | `supplement` | 补配 |
    | 2 | `replaced` | 已替代 |
  * 【已确认】将来若需要更多联合方式（如「可选」「临时替代」），直接在此枚举后追加值即可，不改表结构。


# 注意事项
上述每个表最后都要有以下字段：
* deleted 是否删除，unsigned tinyint，逻辑删除用
* create_time 创建时间
* update_time 更新时间（加上on update）
通用约定补充：
* 所有查询默认追加 `AND deleted=0`（由 `base_dao` 统一处理）；删除统一为软删 `UPDATE ... SET deleted=1`。
* 所有枚举字段统一使用 **`unsigned tinyint` + 列 `COMMENT` 写明数值↔含义映射**（`action`/`object_type` 除外，见日志表）。
时区使用Asia/Shanghai
* 【已确认】具体口径：数据库连接时区设为 `Asia/Shanghai`，**按北京时间存、按北京时间展示**，前端**原样显示、不做任何时区转换**，不提供用户自选时区。

【已确认】**唯一约束的实现方式**：
* 箱子 `name`：同一活动内唯一。
* 箱子 `serial_no`：全局唯一（清空后释放）。
* 活动 `name`：全局唯一。
* 三者均采用**应用层校验**：新建/改名时先 `SELECT ... WHERE ... AND deleted=0` 查重再写入。**不建立包含 `deleted` 维度的 DB 唯一索引**（否则多次软删同名记录会互相冲突）。
* 连带结论：被逻辑删除的记录所占用的名称/串号**可以被重新使用**。
前端URL的要求：
> 因为URL我需要印到箱子上，需要手工输入二维码，所以URL需要简单，以名称为主（而不是UUID等，太难输入了）减少参数使用（“?”问号和“&”符号太难输入），因为网站域名只有一个，所以还需要通过nginx反向代理URL，所以**采用「活动名即前缀」的合并方案**，不再另起 `wb` 之类的固定字符串前缀。URL 形如：
* 查看活动（其下箱子列表）：`/活动名`
* 查看箱子（其下物品列表）：`/活动名/箱子名`，例如 `/2026trip/A01`
* 活动内列表页：`/活动名/xxx-list`

* nginx **按活动名分流**（每个活动一个 `location`，或统一 fallback 到 SPA 由前端路由解析）。
* **活动名全局唯一**，从源头消除了「跨活动同名箱子导致 URL 歧义」的问题，因此 URL 中无需再带活动 ID。
* **URL 编码**：活动名、箱子名作为 URL 路径段时统一做 `encodeURIComponent`；服务端解码后**精确匹配**（区分大小写、不做 slug 转写、不做模糊匹配）。
* **全局页面（不属于任何活动）的 URL 归属**：`登录页`、`活动列表页`、以及跨活动的联合物品/日志列表页无法挂在「活动名」前缀下。我确认：保留一个**保留字前缀**（如 `/_admin/login`、`/_admin/activity-list`），并禁止活动名使用该保留字，这种方式没有问题。

【已确认】**二维码方案**：
* 页面**只提供「复制 URL」按钮**，**不展示二维码图片、不提供下载二维码 PNG 的功能**（后端因此不需要 `qrcode` 依赖，也不需要 `qrcode_controller`）。
* 二维码内容 = **完整 URL**；由用户自行拿这段 URL 去生成二维码并印到箱子上。
* 扫码后直接跳转到「当前活动下该箱子的物品列表页」（即 `/活动名/箱子名`）。
* URL 格式要便于手工录入到第三方二维码生成器 / 便于手工在浏览器输入，**不是**由系统渲染二维码。
* **改名后旧码失效**：箱子或活动改名后，旧二维码指向的 URL 即失效（打开为 404），需要**重新打印**。不做别名表、不做旧 URL 重定向。只要该箱子和活动仍存在，则二维码指向会一直有效。

# 搜索和筛选
每个页面支持模糊搜索和筛选，可以使用关键字搜索物品名称、描述和注意事项，可以筛选type等。
实现口径：
* 搜索与筛选**全部走后端 SQL**，不做前端内存过滤（保证跨页全局命中）。
* 关键字：`LIKE` **参数化**查询 `name` / `desc` / `note` 三个字段（`%关键字%`，值通过参数传入，禁止拼接）。
* 筛选：`type`、`status` 等条件走 `WHERE` 子句；箱子 `type` 为 JSON 多标签，按「包含某标签」过滤。
* 分页在 SQL 层完成（`LIMIT %s OFFSET %s`），每页 **50** 条。
# 列表和分页
* 活动中列举箱子的列表
* 箱子中列举物品的列表
  * 子箱在父箱的物品列表中作为「特殊物品」行出现，操作列提供「查看」以钻取进入。
* 每页50条，多出分页展示
* 对于每个物品：右侧操作列有“编辑”、“取出”、“删除”等操作
* 对于每个箱子：右侧操作列有“查看物品”、“编辑”、“折叠”
  * 折叠仅为状态标志，非空箱也可折叠，前端只做提示不阻断（见「折叠与空箱」）。

* 对于每个活动：右侧操作列有“查看箱子”、“编辑”、“开始/停止活动”
* 编辑页面右上角都有各自“添加（物品、箱子、活动）”等功能。
  * 只读角色（`ro`）在列表页**看不到/不可用**任何写操作按钮（编辑、取出、删除、折叠、开始/停止、添加），后端同时二次校验（见「账号、登录与权限」）。

# 页面风格
1. 界面整体风格以“普通”、“原生样式”为主，根据系统深浅色调整界面深浅主题，或者用户特别制定。字体选择黑体，支持中文显示
2. 要有部分过渡动效，高亮呼吸提示，控件淡入淡出，左右流出合适的margin。
3. 控制按钮意义如果非常清楚，可以使用icon或者图片来代替，普通样式和hover亮度不同，让用户知道这是一个可以交互的按钮。
4. 不能使用任何emoji图标。
5. 移动端必须响应式（触控区放大、窄屏隐藏次要列）；以上风格条款本次确认**保持原样、无变更**。

# 账号、登录与权限【本次确认新增】

## 登录方式
* 有**登录界面**，**无注册界面**（与原文一致）。
* 密码存储：`sha256` + **每用户随机盐**；`salt` 与 `password_hash` 同存 `user` 表。
* 登录成功后，服务端生成随机 token（`secrets.token_urlsafe`）写入 cookie；**cookie 只存这个 token**。
* token ↔ user 的映射落 MySQL **`session` 表**（无 Redis）。
* cookie 属性：`HttpOnly` + `Secure` + `SameSite=Lax`。
* 有效期 **4 小时**，且**滑动续期**：用户正常浏览（任一已鉴权请求）即重置过期时间。

## 角色与权限
* 【已确认】共两个角色，`user` 表新增 **`role`** 字段：

  | 角色 | 说明 |
  |---|---|
  | `rw` | 读写。可查看 + 新建/修改/删除/取出/折叠/开始停止活动等全部写操作 |
  | `ro` | 只读。可登录、可查看与搜索所有列表与详情，但**不能修改任何状态与数据** |
* 管理员账号默认 `rw`。
* `ro` 的限制必须**前后端双重生效**：前端隐藏写操作入口，后端在 service 层校验角色并对写接口返回「无权限」错误。

## 账号生命周期
* **初始管理员**：由 seed 脚本创建，或读取 `.env` 中的 `ADMIN_USER` / `ADMIN_PASS` 创建。无注册界面。
* **修改密码**：支持管理员修改自己的密码，需输入旧密码校验。
* **无「找回密码」功能**（系统不收集邮箱/手机号）。忘记密码只能重跑 seed 脚本重设，或直接改数据库。

## 多端与登出
* 允许**多端同时登录**（同一用户在 `session` 表可有多条有效记录）。
* **不做设备管理页**、**不做「登出全部设备」**。
* 登出只清除**当前** session 记录；其他端仍持有的有效 cookie **照常可用**，直到自身 4 小时过期。

# 照片与文件存储【本次确认新增】

* 照片存**本地 `uploads/` 目录**，数据库**只存相对路径**（不存 blob、不接对象存储）。
* 后端接收上传后用 **Pillow 重新渲染压缩**，并生成 **thumb 缩略图**，两者一并落盘。
* 支持格式：**jpg / png**；上传接口允许类型为 **jpg / png / webp**。
* iPhone 的 **HEIC** 图片通过 **`pillow-heif`** 依赖解码，**转存为 jpg** 后入库（建议在 `requirements.txt` 中加入该依赖）。
* 上传接口**必须登录鉴权**（且需 `rw` 角色）。
* 单文件大小上限 **5MB**。

# 部署与运维【本次确认新增】

* **部署形态：单实例**——一台机器上 `uvicorn` 多 worker + `nginx` 反向代理，自用场景足够。
  * 补充说明：pymysql 是同步驱动，因此涉及 DB 访问的路由统一使用普通 `def` 端点（FastAPI 会自动放入线程池执行），避免阻塞事件循环；多 worker 进一步分摊并发。
* **备份**：提供两个脚本 + 说明文档——
  * `mysqldump` 备份数据库；
  * `rsync` 备份 `uploads/` 目录。
  * 运行方式：**手动执行或自行挂 cron**；**MVP 不内置自动备份调度**。


# 需求注意事项
* 有说得不清楚或者某些边界条件的情况下，不要猜测我的意图。
* 如果有如何问题，可以随时要求我来确认需求，不要思考牛角尖。
* 软件选型使用稳定的版本，不盲目追新。
* 代码架构分明，功能解耦，不允许所有功能混在一起。
* 代码中适当位置（功能模块粒度）要有适当注释，清晰明了。